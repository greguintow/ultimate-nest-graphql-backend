version: '3.7'

services:
  test_db:
    image: postgres:latest
    ports:
      - '5431:5432'
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
